def get_left_iris_landmarks(face):
    return [face.landmark[469],
            face.landmark[470],
            face.landmark[471],
            face.landmark[472]]


def get_right_iris_landmarks(face):
    return [face.landmark[474],
            face.landmark[475],
            face.landmark[476],
            face.landmark[477]]